newWindow = Toplevel(root) 
  

    # newWindow.title("Single song window") 
  
  
    # # A Label widget to show in toplevel 
    # Label(newWindow, text ="This is single song window").pack() 


    # # btnSingle = Button(root,text = "Single Video", command = openNewWindow1)
    # # btnSingle.pack(side= TOP)